# medical.py

class Medical:
    @staticmethod
    def add_apps(app_name, app):
        # Logic to add the app (e.g., register the app in a list, display it, etc.)
        print(f"Added {app_name} app")
        # You can also do any necessary actions like displaying the app
